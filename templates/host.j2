# Tinc host config
# {{ ansible_managed }}

{% for addr in tinc_addresses | d("") %}
Address = {{ addr }}
{% endfor %}

{% for subnet in tinc_subnets | d("") %}
Subnet = {{ subnet }}
{% endfor %}

{{ tinc_ed25519 }}

{{ tinc_rsa }}
